{
  "name": "zuberlin-preconf-devnet-dashboard",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build-client": "turbo build --filter=client",
    "dev": "turbo dev",
    "dev-client": "turbo dev --filter=client",
    "lint": "turbo lint",
    "lint-client": "turbo lint --filter=client",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "latest",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  }
}
